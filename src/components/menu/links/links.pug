div.nav-bar.menu-hide
  ul.menu
    each val in links
      if val.href
        li
          a(href=val.href)= val.label
      else
        li
          button(type='button')= val.label
          ul
            each item in val.links
              li
                a(href=item.href)= item.label
                  
              
